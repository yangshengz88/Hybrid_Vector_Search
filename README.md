# Hybrid Music Search System with ANN HNSW

Build a hybrid music search system that fuses vector similarity(Approximate Nearest Neighbor (ANN) search using the Hierarchical Navigable Small World (HNSW) algorithm) over Spotify's 19-dimensional audio feature vectors with precise metadata constraints in a single query process.